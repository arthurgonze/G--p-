int a;
float b;

typedef struct { 
 int c;
 int z;
} teste;

int calcula(int k, int h) {
      readln( numb1 );
      println(k, "foram calculados");
}

int main() {

  x = y + (u*7);
  soma(calcula(k,7,5,a));

}
